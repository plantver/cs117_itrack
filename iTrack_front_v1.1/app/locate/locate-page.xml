<Page
    class="page"
    navigatingTo="onNavigatingTo"  
    xmlns="http://schemas.nativescript.org/tns.xsd">

    <ActionBar class="action-bar">
        <!-- 
        Use the NavigationButton as a side-drawer button in Android
        because ActionItems are shown on the right side of the ActionBar
        -->
        <NavigationButton ios:visibility="collapsed" icon="res://menu" tap="onDrawerButtonTap"></NavigationButton>
        <!-- 
        Use the ActionItem for IOS with position set to left. Using the
        NavigationButton as a side-drawer button in iOS is not possible,
        because its function is to always navigate back in the application.
        -->
        <ActionItem icon="res://navigation/menu" 
            android:visibility="collapsed" 
            tap="onDrawerButtonTap"
            ios.position="left">
        </ActionItem>
        <Label class="action-bar-title" text="Locate"></Label>
    </ActionBar>
    <DockLayout class="page-content" width="90%" height="85%" backgroundColor="white" stretchLastChild="false">
        <!--<Label dock="top" class="page-icon fa" text="&#xf015;"></Label>-->
        <ActivityIndicator dock="top" busy="{{ isLoading }}" width="75" height="75" />
        <Button dock="top" text="Locate" tap="onLocateTap" class="btn btn-primary btn-active" />
        <ListView dock="top" row="4" vertical-align="middle" items="{{ formatted_display }}" class="list-group" />
    </DockLayout>
    <!--<StackLayout class="page-content">
        <Label verticalAlignment="center" class="page-icon fa" text="&#xf015;"></Label>
        <Button text="Locate" tap="onLocateTap" class="btn btn-primary btn-active" />
        <ListView row="4" items="{{ formatted_display }}" class="list-group" />
    </StackLayout>-->
</Page>
